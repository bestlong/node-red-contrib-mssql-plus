name: CI

on:
  push:
    # branches:
    #   - master
  pull_request:
    branches:
      - master

jobs:
  build:
    steps:
    - uses: actions/checkout@v4

    - name: Start MSSQL containers
      working-directory: ./
      run: docker-compose up -d --build mssql

    - name: test
